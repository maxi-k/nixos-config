#!/usr/bin/env bash
# Cycle between keyboard layouts

if [[ -n "$1" ]]; then
  setxkbmap "$1"
else
  layout=$(setxkbmap -query | grep '^layout:' | awk '{print $2}')
  case $layout in
    us)
      setxkbmap de
      ;;
    *)
      setxkbmap us
      ;;
  esac
fi
